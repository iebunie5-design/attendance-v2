# 🛠 아이디어큐브 출결 관리 시스템 상세 개발 지침서 (v2.1)

이 문서는 개발자가 시스템을 유지보수하거나 기능을 확장할 때 참조해야 할 구체적인 기술 명세와 구현 원리를 담고 있습니다.

---

## 1. 기술 상세 사양 (Tech Stack Details)

### **Frontend**
- **Framework**: `Next.js 14.2.3` (App Router 아키텍처)
- **Runtime**: `Node.js 24.13.0` 이상 권장
- **UI/UX**: 
  - `Framer Motion`: 모든 모달(AnimatePresence) 및 카드 애니메이션 처리
  - `Lucide React`: 시스템 아이콘 일원화
  - `Glassmorphism`: `backdrop-filter: blur`를 활용한 투명 UI 스타일링

### **Backend & Infrastructure**
- **DB/Auth**: `Supabase` (Postgres 기반)
- **QR 처리**: `html5-qrcode` (카메라 제어), `qrcode.react` (QR 코드 생성)

---

## 2. Supabase 연동 및 사용 가이드

본 시스템의 모든 데이터와 인증은 Supabase를 통해 관리됩니다.

### **2.1 프로젝트 설정 (Environment Variables)**
`.env.local` 파일에 다음 정보를 반드시 설정해야 합니다. (Supabase Dashboard -> Project Settings -> API)
```env
NEXT_PUBLIC_SUPABASE_URL=당신의_프로젝트_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=당신의_익명_키(Anon_Key)
```

### **2.2 인증 설정 (Authentication)**
1. **관리자 계정 생성**: Supabase Dashboard -> Authentication -> Users 메뉴에서 관리자 이메일을 직접 추가하거나 소셜 로그인을 활성화하십시오.
2. **비밀번호 정책**: 기본적으로 6자 이상이며, 시스템 내부 `Settings` 페이지에서 변경 가능합니다.

### **2.3 데이터베이스 테이블 생성 (SQL Editor)**
새로운 환경에서 시작할 경우 SQL Editor에 아래 쿼리를 실행하여 테이블을 생성하십시오.
```sql
-- 출결 기록 테이블
create table attendance (
  id bigint generated by default as identity primary key,
  student_id uuid references students(id) on delete cascade,
  class_id uuid references classes(id) on delete cascade,
  date date default current_date,
  status text,
  check_in_time timestamptz default now()
);

-- 반 관리 테이블
create table classes (
  id uuid default gen_random_uuid() primary key,
  name text not null,
  teacher text,
  schedule text,
  room text,
  created_at timestamptz default now()
);

-- 학생 관리 테이블
create table students (
  id uuid default gen_random_uuid() primary key,
  name text not null,
  class_id uuid references classes(id) on delete set null,
  school text,
  grade text,
  parent_contact text,
  qr_code_data text unique,
  created_at timestamptz default now()
);
```

### **2.4 RLS(Row Level Security) 및 정책 설정**
보안을 위해 RLS가 켜져 있다면, 앱에서 데이터를 읽고 쓸 수 있도록 **Policies**를 설정해야 합니다.
1. **조회(SELECT)**: `Authenticated` 사용자에게 모든 권한 부여.
2. **수정/삭제(UPDATE/DELETE)**: 관리자 세션이 있는 경우에만 허용하도록 `auth.uid()` 기반 정책 설정.

---

## 3. 데이터베이스 구조 및 관계 (ERD 명세)

모든 테이블은 Supabase의 `Public` 스키마에 위치합니다.

### **3.1 classes (반 관리 테이블)**
| 컬럼명 | 타입 | 설명 |
| :--- | :--- | :--- |
| `id` | uuid | PK, 자동 생성 |
| `name` | text | 반 이름 (예: 파이썬 중급A) |
| `teacher` | text | 담당 강사명 |
| `schedule` | text | 수업 시간 (예: 토요일 3시) |
| `room` | text | 강의실 호수 |
| `created_at` | timestamptz | 생성 일시 |

### **3.2 students (학생 관리 테이블)**
| 컬럼명 | 타입 | 설명 |
| :--- | :--- | :--- |
| `id` | uuid | PK, 자동 생성 |
| `name` | text | 학생 이름 |
| `class_id` | uuid | FK -> classes.id (반 배정) |
| `school` | text | 소속 학교 |
| `grade` | text | 학년 |
| `parent_contact` | text | 학부모 연락처 |
| `qr_code_data` | text | QR 매칭용 고유 ID (예: `ST-17123456...`) |

### **3.3 attendance (출결 기록 테이블)**
| 컬럼명 | 타입 | 설명 |
| :--- | :--- | :--- |
| `id` | bigint | PK, Identity |
| `student_id` | uuid | FK -> students.id |
| `class_id` | uuid | FK -> classes.id (조회 성능 최적화용) |
| `date` | date | 출결 날짜 (YYYY-MM-DD) |
| `status` | text | 출석, 지각, 결석, 하원 등 |
| `check_in_time` | timestamptz | 실제 기록 시간 (자동) |

---

## 4. 핵심 모듈 구현 코드 분석

### **4.1 출결 상태 토글 시스템 (`src/app/attendance/page.js`)**
사용자가 출석 버튼을 여러 번 누를 때의 로직입니다.
```javascript
// 같은 버튼을 다시 누르면 데이터가 삭제(취소)됩니다.
const updateStatus = (studentId, status) => {
    setAttendanceData(prev => {
        const currentStatus = prev[studentId]?.status;
        const newStatus = currentStatus === status ? null : status; // 토글 로직
        return { ...prev, [studentId]: { ...prev[studentId], status: newStatus } };
    });
};
```

### **4.2 QR 키오스크 로직 (`src/app/qr/page.js`)**
수동 입력 시 `class_id` 누락 문제를 해결한 핵심 로직입니다.
```javascript
// 검색된 학생 객체(full object)를 전달받아 처리합니다.
const handleAttendance = async (student) => {
    const { error } = await supabase.from('attendance').upsert({
        student_id: student.id,
        class_id: student.class_id, // 이 필드가 있어야 통계에 정상 반영됨
        date: new Date().toISOString().split('T')[0],
        status: '출석'
    });
};
```

### **4.3 반별 학생 수 실시간 집계 (`src/app/classes/page.js`)**
단순 숫자가 아닌 DB Join을 통한 자동 계산 로직입니다.
```javascript
const { data, error } = await supabase
    .from('classes')
    .select('*, student_count:students(count)'); 
// Supabase의 Resource Embedding 기능을 활용하여 Join 없이 Count를 가져옵니다.
```

---

## 5. UI/UX 디자인 가이드 (CSS)

### **공통 변수 (`src/app/globals.css`)**
스타일 수정 시 아래 변수를 우선적으로 변경하십시오.
- `--glass-bg`: `rgba(15, 23, 42, 0.8)` (배경 투명도)
- `--primary`: `#6366f1` (메인 인디고 색상)
- `--card-border`: `rgba(255, 255, 255, 0.1)` (카드 경계선)

---

## 6. 트러블슈팅 및 운영 가이드

### **현상: 수정 시 에러가 발생합니다**
- **원인**: Supabase RLS(Row Level Security) 설정이 켜져 있는데 정책(Policy)이 없는 경우 발생합니다.
- **해결**: Supabase Console -> Database -> Policies에서 `classes` 테이블에 대한 `UPDATE` 권한(All roles 또는 Authenticated)을 부여하십시오.

### **현상: QR 카메라가 작동하지 않습니다**
- **원인**: 브라우저 보안 정책으로 인해 `localhost`가 아니거나 `https`가 아닌 환경에서는 카메라 접근이 차단됩니다.
- **해결**: 실제 배포 시 반드시 `https` 도메인을 사용하십시오.

---

## 7. 향후 확장 로직 제안

1. **자동 알림 연동**: `src/lib/sms.js` 파일을 생성하여 외부 API 공급자(Solapi 등)와 연동 한 후, 출결 upsert 성공 시점에 호출 코드를 넣으십시오.
2. **다중 관리자**: `profiles` 테이블을 별도로 만들어 강사별로 담당하는 반만 보이도록 `eq('teacher', current_user_name)` 필터를 추가할 수 있습니다.

---

최종 수정일: 2026년 2월 9일  
작성자: Antigravity AI Toolkit
